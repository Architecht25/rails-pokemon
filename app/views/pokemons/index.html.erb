<h1>Liste des Pokémons</h1>

link for trainers
link for random pokemon
search bar for pokemon


<div class="grid">
  <% @pokemons.each do |pokemon| %>
    <div class="pokemon-card">
      <%= cl_image_tag pokemon.photo.key %>
      <h2><%= link_to pokemon.name.capitalize, pokemon_path(pokemon) %></h2>
      (<%= pokemon.element_type.capitalize %>)
    </div>
  <% end %>
</div>
